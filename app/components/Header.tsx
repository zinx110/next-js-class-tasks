import React from "react";
import Nav from "./Nav";

const Header = () => {
    return (
        <div className="h-10 w-full flex justify-center items-center bg-green-700">
            <Nav />
        </div>
    );
};

export default Header;
